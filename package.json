{
  "name": "embla-carousel-react-component-monorepo",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "docs",
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace docs run dev",
    "build": "yarn workspace embla-carousel-react-component run build"
  },
  "devDependencies": {
    "prettier": "^2.8.3"
  }
}
